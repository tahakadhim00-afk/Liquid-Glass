{
  "name": "liquid-glass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Apple-style Liquid Glass effect for the web: real refraction via WebGL2, with SVG-displacement and CSS-blur fallbacks.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/orientation.test.mjs && node test/lens.test.mjs && node test/light.test.mjs && node test/frost.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^5.4.11"
  }
}
